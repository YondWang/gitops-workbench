tag_list:
  extends: .gitops_base
  stage: query
  rules:
    - if: '$OPERATION == "tag_list"'
  script:
    - api GET "${PROJECT_API}/repository/tags?per_page=100" | { if [ -n "$PATTERN" ]; then grep "$PATTERN" || true; else cat; fi; }

