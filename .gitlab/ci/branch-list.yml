button_branch_list:
  extends: .gitops_base
  variables:
    OPERATION: "branch_list"
  script:
    - |
      query="${PATTERN:+&search=${PATTERN}}"
      api GET "${PROJECT_API}/repository/branches?per_page=100${query}"
