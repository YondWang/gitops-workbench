button_feature_create:
  extends: .gitops_base
  variables:
    OPERATION: "feature_create"
  script:
    - require_version "$VERSION"
    - branch="$(derive_feature_branch)"
    - branch_create "$branch" "baseline/${VERSION}"
    - ok "feature branch ready: ${branch}"
